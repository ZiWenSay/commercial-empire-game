name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker
        run: docker build -t game .
      
      - name: Deploy to Container
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: |
          # Deploy somewhere like Railway or Fly.io
          echo "Deploy action configured"
